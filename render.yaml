# services:
#   - type: web
#     name: amozn
#     env: python
#     plan: free
#     buildCommand: "./build.sh"
#     startCommand: "gunicorn myproject.wsgi:application"
#     routes:
#       - type: rewrite
#         source: "/(.*)"
#         destination: "/"


services:
  - type: web
    name: amozn
    env: python
    plan: free
    buildCommand: "./build.sh"
    # startCommand: "gunicorn myproject.wsgi:application --bind 0.0.0.0:$PORT"
    startCommand: "gunicorn myproject.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --threads 2 --timeout 60"
